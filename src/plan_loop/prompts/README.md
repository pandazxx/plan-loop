# Prompt templates

`plan_task_prompt` and `plan_amend_prompt` live here once designed (see
README.md's Typical workflow, steps 2 and 6). `plan_task_prompt` is the
project's optimization target — treat every change to it as versioned.

Pending detailed design; see ARCHITECTURE.md.
